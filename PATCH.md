# Патч-заметки

Журнал изменений между релизами: что поменяли, зачем и как откатить.
Новое сверху. Пишем сюда сразу при коммите, чтобы на другой машине не
восстанавливать контекст по дифам.

---

## 2026-09-03 — фиксы по фидбеку: крит, обучение, иконки, массовый разбор, окно локации

Сборка `yadryon-pustosh.zip` пересобрана на этом коде (13:29, `npm run pack`:
78 файлов, 8.9 МБ). Версия в `package.json` не менялась — 0.3.0. На модерацию
ещё НЕ уходило.

Проверено перед коммитом: `npx eslint src sim tests` чист, `npm test` — 141 тест
зелёный, `npm run sim` и `npm run sim:depth` совпадают с числами прошлой сборки
(глубинный рамп включается с 30-й локации, симуляторы дальше 19-й не заходят).

### 1. Крит-шанс упирался в 90%, и об этом нигде не говорилось
Файлы: `src/data/balance.js`, `src/state/GameState.js`, `src/scenes/BattleScene.js`,
`src/scenes/HeroScene.js`, `src/data/upgrades.js`, `src/i18n.js`, оба симулятора.

Симптом (фидбек): «шанс крита не поднимается выше 90%, как ты его ни качай; если
крит-урон при этом растёт — это нигде не сказано». Он и не рос: потолок `0.9`
стоял прямо в `critChance()` числом, всё сверх него пропадало молча. А упереться
в него легко — `pointsPerLevel: 3` очка за уровень по `perLuckCrit: 0.01`, то
есть примерно к 30-му уровню при вкладе в удачу.

Стало: потолок вынесен в `BAL.critChanceCap`, перелив сверх него идёт в
КРИТ-УРОН — `BAL.critOverflowToMult: 2.0` («+2.0 к множителю за каждые 100%
сверх потолка», то есть +0.02 за 1%). В состоянии появились `critChanceRaw()`,
`critOverflow()`, `critMultiplier()`; бой умножает урон на `State.critMultiplier()`
вместо константы. Обе сводки статов (бой и «Герой») показывают теперь
`Крит: 90% ×3.38`, описания удачи и «Меткого глаза» говорят про перелив.

**Откат**: `critOverflowToMult: 0` — перелив снова пропадает, потолок остаётся.

### 2. Обучение не упоминало реликвии
Файл: `src/scenes/HubScene.js`. На вторую страницу «Как играть» добавлена строка
«Босс 10-й локации роняет части реликвии — собери 5 и куй». Шаг строк ужат
68 → 62, иначе пятая налезала на кнопки.

### 3. На верстаке показывались эмодзи вместо арта снаряжения
Файлы: `src/ui/scenery.js`, `src/scenes/ForgeScene.js`, `src/scenes/InventoryScene.js`.

Результат ковки рисовался через `slot.icon` (эмодзи 🔫/⛑️), хотя инвентарь давно
показывает арт (`weapon-N` / `slot-*`). Хелпер иконки предмета переехал из
инвентаря в `ui/scenery.js` как `itemIcon()` и используется обоими экранами.

### 4. Массовый разбор и продажа рюкзака (запрос)
Файлы: `src/data/loot.js`, `src/state/GameState.js`, `src/scenes/InventoryScene.js`.

Под списком добычи теперь три кнопки: «Хлам и годное» (как было, без вопросов),
«Всё в лом» и «Продать всё» — обе с подтверждением, в котором названо число
предметов и точная сумма. Надетое недосягаемо по устройству: экипировка лежит в
`equipment`, а не в `inventory`. Цена продажи вынесена в `sellValue()` (была
зашита в `sellItem`), добавлены `bulkValue()`, `scrapAll()`, `sellAll()`.

### 5. Окно «локация взята»: идти дальше или ×2 крышек за ролик (запрос)
Файлы: `src/scenes/BattleScene.js`, `src/state/GameState.js`.

После босса-ворот вместо мгновенного продолжения открывается окно с добытыми в
этой локации крышками и выбором: «Идти дальше» или «📺 Реклама: ×2». Копилка
локации — `capsInZone` (только боевые крышки, `addBattleCaps`; продажа лута и
офлайн-доход мимо неё), на взятии зоны она закрывается в `lastZoneCaps`.

Поток врагов на время окна выключен (`clearEnemies`) и запускается заново по
кнопке — иначе враги били бы героя, пока игрок читает награду. Оценка игры и
межстраничная реклама (`askReviewOrAd`) остались только на «мирном» выходе:
после ролика за награду вторая реклама подряд недопустима.

### 6. Глубинный рамп (компенсация к п.5)
Файлы: `src/data/balance.js`, `src/state/GameState.js`, `src/scenes/BattleScene.js`, оба симулятора.

Ступени локаций (`ZONE_STEPS`) поднимали только HP; урон и скорость от номера
зоны не зависели вовсе, а ×2 крышек за ролик ещё и удваивает доход на глубине.

С локации `deepZoneStart: 30` включаются ДВА рампа, и они перемножаются:

1. **за локацию** — `deepZoneRamp: 1.02` за каждую следующую (ступенька на входе);
2. **за убийства** — `deepKillRamp: 1.02` за каждые `deepKillStep: 20` убийств,
   сделанных на глубине (ровный рост ВНУТРИ локации, устроен как `killScaleRamp`).

Оба идут к HP, урону И скорости. Второй считает не `totalKills`, а свой счётчик
`GameState.deepKills` — только убийства с 30-й локации и глубже: от `totalKills`
на входе в 30-ю набралось бы ~3000 убитых, то есть ×19 сразу, стена вместо
рампа. Счётчик лежит в сейве и обнуляется перерождением (`resetRun`).

Скорость режется потолком `deepSpeedCap: 2.5` — выше него враг пересекает арену
быстрее, чем игрок успевает кликнуть.

Во что это выливается (при `zoneKills: 100`, то есть 101 убийство на локацию —
одна локация глубины = ×1.02 × ×1.02⁵ = **×1.126**):

| Локация | Глубже старта | Убийств на глубине | HP и урон | Скорость |
|---|---|---|---|---|
| 30 | 0 | 0 | ×1.00 | ×1.00 |
| 35 | 5 | 505 | ×1.81 | ×1.81 |
| 38 | 8 | 808 | ×2.59 | ×2.50 (потолок) |
| 40 | 10 | 1010 | ×3.28 | ×2.50 |
| 50 | 20 | 2020 | ×11.0 | ×2.50 |
| 60 | 30 | 3030 | ×36.0 | ×2.50 |

Рамп множится НА существующие: `killScaleRamp` (+1% HP за 20 убийств) и
`killScaleRampDmg` (+0.5% урона) продолжают работать от `totalKills`. Итого за
20 убийств на глубине HP ×1.0302, урон ×1.0251 — плюс ×1.02 на входе в локацию.

**Откат**: `deepZoneRamp: 1` выключает рамп за локацию, `deepKillRamp: 1` — за
убийства (параметры можно оставить, при 1 они не делают ничего).

Симуляторы (`npm run sim`, `npm run sim:depth`) в своих окнах наблюдения до 30-й
локации не доходят (максимум 19-я) — их числа не изменились ни на единицу.

---

## 2026-09-02 — A/B по темпу зоны, реклама вне iframe, фиксы кликспама

Сборка `yadryon-pustosh.zip` пересобрана на этом коде (13:42, после всех правок).
Версия в `package.json` не менялась — 0.3.0. На модерацию ещё НЕ уходило.

### 1. A/B-эксперимент баланса: короткая зона + сквозной рамп по убийствам
Файлы: `src/data/balance.js`, `src/data/scaling.js`.

Идея: босс должен приходить быстрее, а давление создаёт не длина зоны, а
постоянный рост силы врагов за весь забег.

| Параметр | Было | Стало | Зачем |
|---|---|---|---|
| `zoneKills` | 400 | 100 | зона вчетверо короче, босс-ворота ближе |
| `enemyBaseReward` | 6 | 6.6 | +10% дохода, чтобы догонять рамп |
| `enemySpeed` | 70 | 90 | враги доходят до героя заметно быстрее |
| `killScaleStep` | — | 20 | каждые N убийств за забег… |
| `killScaleRamp` | — | 1.01 | …+1% к HP врагов |
| `killScaleRampDmg` | — | 1.005 | …+0.5% к урону (мягче HP, чтобы не спираль смерти) |

Как работает рамп (`enemyStats` в `scaling.js`): `rampSteps = floor(stage / killScaleStep)`,
HP умножается на `killScaleRamp^rampSteps`, урон на `killScaleRampDmg^rampSteps`.
Награду рамп НЕ трогает — это намеренно, доход отстаёт от сложности.
`stage` = `totalKills`, поэтому рамп идёт сквозь зоны и смерти, в отличие от
`enemyWaveRamp` (сбрасывается каждой зоной). Обнуляется только перерождением
(`resetRun` зануляет `totalKills`). `prestigeEnemyMul` не менялся.

**Откат эксперимента**: вернуть `zoneKills: 400`, `enemyBaseReward: 6`,
`enemySpeed: 70`, а рамп выключить через `killScaleRamp: 1` и
`killScaleRampDmg: 1` (параметры можно оставить, при 1 они ничего не делают).
Перед откатом или продолжением прогнать `npm run sim` и `npm run sim:stability`.

### 2. Реклама вне iframe Яндекса больше не «вешает» игру
Файл: `src/platform/yandex.js`.

Симптом: на localhost или при прямом открытии сборки смена зоны зависала на
~15 секунд. Причина: SDK подгружается и `available=true`, но игра не в iframe
платформы, `postMessage` слать некому («No parent to post message»), колбэки
`onOpen/onClose/onError` не приходят, а пауза из `_adStart` висит до
15-секундного сторожа.

Решение: метод `_embedded()` (`window.self !== window.top`). Если не в iframe,
`showRewarded` сразу выдаёт награду и закрывается, `showInterstitial` не
показывается вовсе. На платформе поведение не меняется.

### 3. Твины при кликспаме (фидбек игроков)
Файлы: `src/scenes/BattleScene.js`, `src/scenes/ShopScene.js`.

- **Герой уезжал за левый край** при быстрых кликах (чаще ~90 мс). Отдача
  выстрела шла от ТЕКУЩЕЙ позиции, предыдущий твин не успевал вернуть героя.
  Теперь есть якорь `heroBaseX`, старый твин снимается, позиция сбрасывается
  к якорю до и после отдачи.
- **Враг оставался приплюснутым** после серии попаданий: та же природа,
  наложение сквош-твинов. Теперь `punchTween` снимается, масштаб сбрасывается
  к `dispScale` до и после.
- **Цена в магазине «застревала» раздутой** при спаме по недоступной покупке.
  В `flash()` флаг `_flashing`: новый твин не запускается, пока идёт старый.

### 4. Лидерборд: десятая строка налезала на кнопку «В лагерь»
Файл: `src/scenes/LeaderboardScene.js`.

Верх кнопки — 651px. Раньше сетка начиналась с 150 при шаге 52, десятая строка
заканчивалась на 664px. Теперь старт 130, шаг 48, высота панели 44, низ
десятой строки — 606px.

### Проверка
`npm test` — 126 проверок в 6 файлах, всё зелёное.
