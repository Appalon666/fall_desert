# Права на материалы (ответ модерации по п.3.5)

Модерация запросила подтверждение прав на используемые материалы. Здесь собрано
всё, что можно подтвердить по репозиторию, и отдельно отмечено то, что должен
подтвердить автор — **эти места нужно заполнить перед отправкой ответа**.

---

## 1. Графика: враги, боссы, союзник, герои, фоны

**Сгенерирована нейросетью по авторским промтам.** Промты целиком лежат в
репозитории — `ART-PROMPTS.md` (16 КБ, по разделу на каждое существо), исходные
генерации — в `art-in/`, нарезанные спрайт-листы — в `public/sprites/` и
`public/bg/`. Конвейер нарезки и сжатия — `tools/run-mobs.ps1`, `tools/mob-cut.py`,
`tools/png8.py`.

То есть по каждому спрайту прослеживается цепочка: промт → исходник → нарезка →
файл в сборке. Ни одно изображение не взято из чужой игры или стока.

> ⚠️ Заполнить: указать конкретный сервис генерации и тарифный план, по которому
> права на результат принадлежат пользователю. В `ART-PROMPTS.md` упомянут
> **Nano Banana**. Приложить к ответу ссылку на условия использования сервиса.

## 2. Интерфейс, иконки, эффекты

Рисуются кодом на лету (`src/assets.js`, `src/gfx/fx.js`, `src/ui/scenery.js`) либо
входят в тот же генерационный конвейер (`public/ui/`, `public/icons/`,
`public/weapons/`). Готовых UI-китов со стороны не использовалось.

## 3. Шрифт

**Rubik**, самохостинг — `public/fonts/rubik-*.woff2`, подключается через
`@font-face` в `index.html`, внешних CDN нет. Rubik распространяется по
**SIL Open Font License 1.1**, которая разрешает коммерческое использование и
встраивание.

> ⚠️ Заполнить: положить рядом со шрифтом файл лицензии `OFL.txt` (берётся со
> страницы шрифта в Google Fonts) — модерации проще, когда лицензия лежит в
> сборке, а не только упоминается.

## 4. Звук

Звуковые эффекты **синтезируются в рантайме** через WebAudio (`src/audio/sfx.js`) —
это код, а не файлы, вопрос прав к ним не возникает.

**Музыка — единственное место, где нужен внешний источник.** В сборке два файла:
`public/music/menu.ogg` и `public/music/battle.ogg`.

**Что удалось выяснить из самих файлов** (теги внутри ogg):

| Файл | Метаданные | Вывод |
|---|---|---|
| `menu.ogg` | `Adobe Soundbooth CS5`, дата `2013-08-29` | сделан в 2013 году в чужой программе |
| `battle.ogg` | `This song has been made using Linux MultiMedia Studio` | тег, который LMMS ставит автоматически |

Ни автора, ни названия, ни лицензии в тегах нет. То есть оба трека **скачаны
со стороны**, а не написаны автором игры, и подтвердить лицензию по файлам
невозможно.

> ⚠️ **Это наиболее вероятная причина замечания по п.3.5.** Нужно либо вспомнить
> страницу, откуда треки скачаны, и приложить ссылку с названием лицензии, либо
> заменить оба на заведомо CC0.
>
> Важно: «бесплатно скачал» модерацию не устроит. Два самых частых варианта
> бесплатной музыки не подходят: **CC BY** требует указания автора в игре или
> описании, **CC BY-NC** запрещает коммерческое использование — а в игре есть
> реклама. Годится CC0 / public domain либо платная лицензия с правом
> коммерческого использования.
>
> Замена ничего не ломает: движок молча работает без музыки, если файлов нет
> (`src/audio/music.js`). Архив при этом худеет с 8.9 до 5.9 МБ.

Кандидаты на замену (проверьте лицензию на странице загрузки) — в
`dock/music-sources.md`. Раньше этот список лежал в `public/music/README.md` и
**уезжал внутрь игрового архива**: модератор, листающий файлы игры, читал
заметку «проверь лицензию перед заливкой». Перенесён в `dock/`.

---

## Шаблон ответа модерации

```
Здравствуйте! Подтверждаю права на материалы игры:

1. Вся графика (персонажи, враги, боссы, фоны, интерфейс) создана мной с помощью
   нейросети <СЕРВИС> по авторским текстовым промтам. Промты и исходные
   генерации сохранены; по каждому спрайту прослеживается цепочка от промта до
   файла в сборке. Условия сервиса передают права на результат пользователю:
   <ССЫЛКА НА УСЛОВИЯ>.

2. Шрифт Rubik используется по лицензии SIL Open Font License 1.1, разрешающей
   коммерческое использование. Шрифт самохостится, внешних CDN нет.

3. Звуковые эффекты синтезируются кодом игры в рантайме (WebAudio), это не
   заимствованные файлы.

4. Фоновая музыка: <ИСТОЧНИК>, лицензия <ЛИЦЕНЗИЯ>, ссылка: <ССЫЛКА>.

Готов предоставить дополнительные материалы по запросу.
```
